# Copy to .env.local for local testing with `vercel dev`. Never commit the real file.
# In production, set these in Vercel: Project → Settings → Environment Variables.
GEMINI_API_KEY=
# Optional. Defaults to gemini-2.5-flash.
GEMINI_MODEL=
